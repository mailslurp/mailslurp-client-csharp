/*
 * MailSlurp API
 *
 * MailSlurp is an API for sending and receiving emails and SMS from dynamically allocated email addresses and phone numbers. It's designed for developers and QA teams to test applications, process inbound emails, send templated notifications, attachments, and more.  ## Resources  - [Homepage](https://www.mailslurp.com) - Get an [API KEY](https://app.mailslurp.com/sign-up/) - Generated [SDK Clients](https://docs.mailslurp.com/) - [Examples](https://github.com/mailslurp/examples) repository
 *
 * The version of the OpenAPI document: 6.5.2
 * Contact: contact@mailslurp.dev
 * Generated by: https://github.com/openapitools/openapi-generator.git
 */


using System;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.Linq;
using System.IO;
using System.Runtime.Serialization;
using System.Text;
using System.Text.RegularExpressions;
using Newtonsoft.Json;
using Newtonsoft.Json.Converters;
using Newtonsoft.Json.Linq;
using System.ComponentModel.DataAnnotations;
using OpenAPIDateConverter = mailslurp.Client.OpenAPIDateConverter;

namespace mailslurp.Model
{
    /// <summary>
    /// Authenticated saved email audit request
    /// </summary>
    [DataContract(Name = "CreateEmailAuditOptions")]
    public partial class CreateEmailAuditOptions : IValidatableObject
    {
        /// <summary>
        /// Initializes a new instance of the <see cref="CreateEmailAuditOptions" /> class.
        /// </summary>
        /// <param name="fromAddress">Optional sender address context.</param>
        /// <param name="recipient">Optional recipient context.</param>
        /// <param name="subject">Optional subject line context.</param>
        /// <param name="htmlBody">Optional HTML email body.</param>
        /// <param name="textBody">Optional text email body.</param>
        /// <param name="emailAnalysis">emailAnalysis.</param>
        /// <param name="hasAttachments">Whether the source email included attachments.</param>
        public CreateEmailAuditOptions(string fromAddress = default, string recipient = default, string subject = default, string htmlBody = default, string textBody = default, EmailAnalysis emailAnalysis = default, bool? hasAttachments = default)
        {
            this.FromAddress = fromAddress;
            this.Recipient = recipient;
            this.Subject = subject;
            this.HtmlBody = htmlBody;
            this.TextBody = textBody;
            this.EmailAnalysis = emailAnalysis;
            this.HasAttachments = hasAttachments;
        }

        /// <summary>
        /// Optional sender address context
        /// </summary>
        /// <value>Optional sender address context</value>
        /*
        <example>sender@example.com</example>
        */
        [DataMember(Name = "fromAddress", EmitDefaultValue = true)]
        public string FromAddress { get; set; }

        /// <summary>
        /// Optional recipient context
        /// </summary>
        /// <value>Optional recipient context</value>
        /*
        <example>inbox@example.net</example>
        */
        [DataMember(Name = "recipient", EmitDefaultValue = true)]
        public string Recipient { get; set; }

        /// <summary>
        /// Optional subject line context
        /// </summary>
        /// <value>Optional subject line context</value>
        [DataMember(Name = "subject", EmitDefaultValue = true)]
        public string Subject { get; set; }

        /// <summary>
        /// Optional HTML email body
        /// </summary>
        /// <value>Optional HTML email body</value>
        [DataMember(Name = "htmlBody", EmitDefaultValue = true)]
        public string HtmlBody { get; set; }

        /// <summary>
        /// Optional text email body
        /// </summary>
        /// <value>Optional text email body</value>
        [DataMember(Name = "textBody", EmitDefaultValue = true)]
        public string TextBody { get; set; }

        /// <summary>
        /// Gets or Sets EmailAnalysis
        /// </summary>
        [DataMember(Name = "emailAnalysis", EmitDefaultValue = true)]
        public EmailAnalysis EmailAnalysis { get; set; }

        /// <summary>
        /// Whether the source email included attachments
        /// </summary>
        /// <value>Whether the source email included attachments</value>
        [DataMember(Name = "hasAttachments", EmitDefaultValue = true)]
        public bool? HasAttachments { get; set; }

        /// <summary>
        /// Returns the string presentation of the object
        /// </summary>
        /// <returns>String presentation of the object</returns>
        public override string ToString()
        {
            StringBuilder sb = new StringBuilder();
            sb.Append("class CreateEmailAuditOptions {\n");
            sb.Append("  FromAddress: ").Append(FromAddress).Append("\n");
            sb.Append("  Recipient: ").Append(Recipient).Append("\n");
            sb.Append("  Subject: ").Append(Subject).Append("\n");
            sb.Append("  HtmlBody: ").Append(HtmlBody).Append("\n");
            sb.Append("  TextBody: ").Append(TextBody).Append("\n");
            sb.Append("  EmailAnalysis: ").Append(EmailAnalysis).Append("\n");
            sb.Append("  HasAttachments: ").Append(HasAttachments).Append("\n");
            sb.Append("}\n");
            return sb.ToString();
        }

        /// <summary>
        /// Returns the JSON string presentation of the object
        /// </summary>
        /// <returns>JSON string presentation of the object</returns>
        public virtual string ToJson()
        {
            return Newtonsoft.Json.JsonConvert.SerializeObject(this, Newtonsoft.Json.Formatting.Indented);
        }

        /// <summary>
        /// To validate all properties of the instance
        /// </summary>
        /// <param name="validationContext">Validation context</param>
        /// <returns>Validation Result</returns>
        IEnumerable<ValidationResult> IValidatableObject.Validate(ValidationContext validationContext)
        {
            // FromAddress (string) maxLength
            if (this.FromAddress != null && this.FromAddress.Length > 320)
            {
                yield return new ValidationResult("Invalid value for FromAddress, length must be less than 320.", new [] { "FromAddress" });
            }

            // FromAddress (string) minLength
            if (this.FromAddress != null && this.FromAddress.Length < 0)
            {
                yield return new ValidationResult("Invalid value for FromAddress, length must be greater than 0.", new [] { "FromAddress" });
            }

            // Recipient (string) maxLength
            if (this.Recipient != null && this.Recipient.Length > 320)
            {
                yield return new ValidationResult("Invalid value for Recipient, length must be less than 320.", new [] { "Recipient" });
            }

            // Recipient (string) minLength
            if (this.Recipient != null && this.Recipient.Length < 0)
            {
                yield return new ValidationResult("Invalid value for Recipient, length must be greater than 0.", new [] { "Recipient" });
            }

            // Subject (string) maxLength
            if (this.Subject != null && this.Subject.Length > 998)
            {
                yield return new ValidationResult("Invalid value for Subject, length must be less than 998.", new [] { "Subject" });
            }

            // Subject (string) minLength
            if (this.Subject != null && this.Subject.Length < 0)
            {
                yield return new ValidationResult("Invalid value for Subject, length must be greater than 0.", new [] { "Subject" });
            }

            // HtmlBody (string) maxLength
            if (this.HtmlBody != null && this.HtmlBody.Length > 200000)
            {
                yield return new ValidationResult("Invalid value for HtmlBody, length must be less than 200000.", new [] { "HtmlBody" });
            }

            // HtmlBody (string) minLength
            if (this.HtmlBody != null && this.HtmlBody.Length < 0)
            {
                yield return new ValidationResult("Invalid value for HtmlBody, length must be greater than 0.", new [] { "HtmlBody" });
            }

            // TextBody (string) maxLength
            if (this.TextBody != null && this.TextBody.Length > 200000)
            {
                yield return new ValidationResult("Invalid value for TextBody, length must be less than 200000.", new [] { "TextBody" });
            }

            // TextBody (string) minLength
            if (this.TextBody != null && this.TextBody.Length < 0)
            {
                yield return new ValidationResult("Invalid value for TextBody, length must be greater than 0.", new [] { "TextBody" });
            }

            yield break;
        }
    }

}
